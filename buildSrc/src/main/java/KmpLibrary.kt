/**
 * Shared Kotlin Multiplatform library defaults for Demo Skeleton core/feature/platform modules.
 * Kept minimal — each module still owns its build.gradle.kts.
 */
object KmpLibrary {
    const val ANDROID_NAMESPACE_PREFIX = "com.example.kuikly"
    const val COMPILE_SDK = 34
    const val MIN_SDK = 21
}
