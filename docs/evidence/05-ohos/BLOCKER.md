# Ohos evidence blocker

Toolchain present (DevEco, hdc targets Connected including USB device).  
`libshared.so` missing for native link (`ohosApp/entry/libs/arm64-v8a/libshared.so`) — need Ohos shared link / signing pipeline in DevEco before visual Auth+Feed screenshots.

See toolchain-check.log / hvigor-assemble.log.
