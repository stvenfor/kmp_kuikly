# Slice-13: Platform extensions as required by source
**Status:** accepted (continuous)  
Only thin Platform Extension stubs already needed by prior slices (permission/share already present). Fill expect/actual gaps for Ohos if any product path calls them; no new product domain. Skip if already covered — document evidence and PASS thin.
