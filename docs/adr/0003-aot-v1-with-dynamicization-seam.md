# AOT for v1, Dynamicization Seam only

v1 ships with the app (AOT / bundled artifacts). We deliberately do not build OTA/dynamic page delivery yet, but keep Module and bundle boundaries so a later phase can add dynamicization without rewriting Feature APIs.
