# Compose primary with Legacy DSL Track

Teaching Demo Skeleton needed both paradigms visible, but one primary stack for long-term samples. We used Kuikly Compose DSL for formal Feature pages and kept a small Legacy DSL Track (`Pager` / `ViewBuilder`) for contrast only — not a second product UI stack.

**Status:** partially superseded by [ADR-0005](./0005-my-ai-project-migration-vertical-slices.md) for **product** pages and the default launch path: product UI is Compose-only; Legacy DSL is not required on the product path or product golden gate. Leftover Legacy samples may remain only as non-default / debug until removed.
