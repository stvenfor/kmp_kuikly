# Compose primary with Legacy DSL Track

Teaching Demo Skeleton needs both paradigms visible, but one primary stack for long-term samples. We use Kuikly Compose DSL for formal Feature pages and keep a small Legacy DSL Track (`Pager` / `ViewBuilder`) for contrast only — not a second product UI stack.
